each map filter reject reduce sort sort_by reverse flatten uniq count size empty? any? all? none? one? first last min max sum abs ceil floor round to_i to_s to_f to_a to_h split join chars bytes slice slice_before slice_after slice_when index find grep scan sub gsub upcase downcase swapcase capitalize strip delete tr match start_with? end_with? each_with_object group_by chunk chunk_while partition tally transpose zip rotate product permutation combination take drop compact fill bsearch bsearch_index digits even? odd? zero? positive? negative? between? step times upto downto loop break next return rescue begin end def if unless else elsif while until for in do then tap true false nil and or not && || -= /= *= ||= Complex to_c 1i rect gcd lcm gcdlcm minmax abs2 conj imag real to_r to_d quo bit_length filter_map flat_map each_cons each_slice cycle with_index with_object lazy to_enum repeated_permutation repeated_combination uniq! sort_by! min_by max_by minmax_by clamp nil? frozen? freeze dup to_set Hash Math::PI Marh::E Integer.sqrt Math.log Math.log2 Math.log10 Math.sin Math.cos Math.tan Math.asin Math.acos Math.atan Math.atan2 require 'prime' Prime prime? 'matrix' Matrix[] Vector[] det rank trace inv row column map! match? =~ !~ $` tr_s delete_prefix delete_suffix delete! squeeze squeeze! count_while rindex slice! []= insert prepend << push pop shift unshift dig >> + - _1 _2 += .any?(_1) ? : prev numerator denominator /\d/
